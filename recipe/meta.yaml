{% set name = "elfutils" %}
{% set version = "0.171" %}
{% set sha256 = "cce883ecffe5d0a3bb19dc23645af74c6f1166727ee6bec3cee741ffd938f066" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.bz2
  url: https://fedorahosted.org/releases/e/l/elfutils/{{ version }}/{{ name }}-{{ version }}.tar.bz2
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [not linux]

requirements:
  build:
    - toolchain
    - zlib 1.2.*
    - m4

  run:
    - zlib 1.2.*

about:
  home: https://fedorahosted.org/elfutils/
  license: LGPL-3.0
  summary: 'a set of utilities and libraries for handling ELF (Executable and Linkable Format) files.'

extra:
  recipe-maintainers:
    - edisongustavo
